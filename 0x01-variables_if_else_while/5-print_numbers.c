#include <stdio.h>

/**
 * main - prints allsingle digit numbers of base 10
 *
 * Return:always 0 (success)
 *
 */
int main(void)
{
	int x;

	while (x < 10)
	{
		printf("%d", x);
		x++;
	}
	return (0);
}
