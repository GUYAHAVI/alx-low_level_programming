#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>

/**
 * main - prints single digit numbers of base 10 followed by a new line
 *
 * Return: 0 (success)
 *
 */
int main(void)
{
	int x;

	do {
		putchar(x);
		x++;
		putchar('\n');
	}
	while (x < 10)
	return (0);
}
