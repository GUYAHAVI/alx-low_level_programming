#include "main.h"
#include <string.h>

/**
*_strlen- prints the length of a string
*
*@s: string whose length is needed
*
*Return: length of a string
*
*/
int _strlen(char *s)
{
	return (strlen(s));
}
